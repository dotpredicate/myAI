
# Repositories

Repositories are sources of data you share with Agents. 

## Supported types 

- plain folders
- Git repositories

## Security

Security policies (global defaults):
- read-only
- privileged-write
- write

## Indexing

Readable text files are saved into a document index.

In Git repos, additionally only the files returned by `git ls-files` are indexed.